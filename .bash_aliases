#!/usr/bin/env bash

alias edit='vim';

alias finder='open -a Finder ./';

alias ll='ls -alF'
alias lh='ls -lah'
alias lt='ls -ltr'
alias la='ls -A'
alias l='ls -CF'

alias ip='curl http://checkip.amazonaws.com/';
